spring:
  datasource:
    url: jdbc:h2:mem:testdb

  flyway:
    enabled: false

  security:
    oauth2:
      client:
        registration:
          google:
            client-id: test
            client-secret: test