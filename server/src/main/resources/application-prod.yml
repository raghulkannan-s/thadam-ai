server:
  port: ${SERVER_PORT:5000}

spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}



  jpa:
    show-sql: false

  flyway:
    enabled: true

  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${GOOGLE_CLIENT_ID}
            client-secret: ${GOOGLE_CLIENT_SECRET}

jwt:
  secret: ${JWT_SECRET}
  expiration: ${JWT_EXPIRATION:900000}
  refresh-expiration: ${JWT_REFRESH_EXPIRATION}

app:
  frontend:
    url: ${FRONTEND_URL:https://roadmap.raghul.dev}

  auth:
    refresh-cookie:
      secure: true
      same-site: None

  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:https://roadmap.raghul.dev}

logging:
  level:
    root: WARN
    com.thadam: INFO